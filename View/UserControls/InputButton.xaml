<UserControl x:Class="FDPortal.View.UserControls.InputButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d">
    <Button Margin="0 20" Style="{StaticResource primaryButton}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Login" Grid.Column="0" Margin="2 0"/>
            <Icon:PackIconMaterial Kind="Login" Grid.Column="2" Margin="2 0"/>
        </Grid>
    </Button>
</UserControl>
